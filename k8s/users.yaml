---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-deployment
  labels:
    app: users
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users
  template:
    metadata:
      labels:
        app: users
    spec:
      containers:
      - name: users
        image: 22i0837/lightstep-users:6a71e7ce23677348a1014a0394ef563c053c5d5e
        ports:
        - containerPort: 8080
        env:
        - name: DBHOST
          value: "mongodb://mongo-service:27017"

---
apiVersion: v1
kind: Service
metadata:
  name: users-service
spec:
  type: ClusterIP
  selector:
    app: users
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
